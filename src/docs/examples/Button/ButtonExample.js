import React from "react";
import Button from "react-ultra-instinct/Button";

export default () => {
  return (
    <Button disabled onClick={() => console.log("hello there")}>
      Link
    </Button>
  );
};
